model User {
    id  String @id @default(uuid())
    name String
    email String @unique
    password String 
    phone String?
    address String?
    profilePhoto String?

    role  Role @default(USER)
    status UserStatus  
}